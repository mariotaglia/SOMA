This file explains how to set up SOMA on your "local" machine

# # # # # # # # #
Set compiler
# # # # # # # # #
	setups/theorie.sh						(!!!Check with your local admin)
	check pgicc
	check mpicc -> error set
		edit .bashrc and add:
		PATH=/usr/lib64/openmpi/bin:$PATH			(!!!Check with your local admin)
		LD_LIBRARY_PATH=/usr/lib64/openmpi/lib:$LD_LIBRARY_PATH (!!!Check with your local admin)

# # # # # # # # # #
Use your own hdf5
# # # # # # # # # #
	build hdf5 library
		cd hdf5
		mkdir install
		CC=mpicc ./configure --enable-parallel --prefix=/path/to/gpuhack-matters/hdf5/install
		make install

# # # # # # # # # # #
SOMA Installation:
# # # # # # # # # # #
	mkdir build
	cd build
	HDF5_ROOT=/path/to/gpuhack-matters/hdf5/install  cmake -DCMAKE_INSTALL_PREFIX=/path/to/gpuhack-matters/install ../
	ccmake . 							(!!!to see more options)
	make
	make test
	make install

# # # # # # # # # # # # # # # #
Umgebungsvariablen hinzufügen
# # # # # # # # # # # # # # # #
	open vim .bashrc and add the following line:
	LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/path/to/gpuhack-matters/hdf5/install/lib/
